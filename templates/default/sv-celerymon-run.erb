#!/bin/bash
exec 2>&1

if test -f /etc/default/celeryd; then
  . /etc/default/celeryd
fi

if [ -n "$VIRTUALENV" ]; then
  . $VIRTUALENV/bin/activate; $cmd
fi

exec $CELERYD_CHDIR/manage.py celerymon


